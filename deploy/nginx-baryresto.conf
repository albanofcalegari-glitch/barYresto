# baryresto — Nginx reverse proxy for Cloudflare
# Place in /etc/nginx/sites-available/ and symlink to sites-enabled

# Public site: baryresto.qngine.com.ar
server {
    listen 80;
    listen 443 ssl;
    server_name baryresto.qngine.com.ar;

    ssl_certificate /etc/nginx/ssl/baryresto.crt;
    ssl_certificate_key /etc/nginx/ssl/baryresto.key;

    location / {
        proxy_pass http://127.0.0.1:3004;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# Admin dashboard: baryresto-admin.qngine.com.ar
server {
    listen 80;
    listen 443 ssl;
    server_name baryresto-admin.qngine.com.ar;

    ssl_certificate /etc/nginx/ssl/baryresto.crt;
    ssl_certificate_key /etc/nginx/ssl/baryresto.key;

    location / {
        proxy_pass http://127.0.0.1:3004;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
